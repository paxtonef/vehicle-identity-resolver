"""Packaged configuration resources (YAML) for the Vehicle Identity Resolver.

These files are required runtime configuration, not optional assets. They are
loaded and validated eagerly by :mod:`vir.config` at import time — see
``vir.config.ConfigurationError`` for the failure modes this enforces.
"""
